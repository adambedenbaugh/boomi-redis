<project name="BoomiCacheConnector" basedir="." default="package">
	<buildnumber file="build.num"/>
	
	<property name="version.num" value="0.88"/>
	
	<property name="class.dir" location="bin" />
	<property name="inner-jar.name" value="BoomiCacheConnector-${version.num}.jar" />
	<property name="target.dir" location="target" />
	<property name="embed-resources.dir" location="src/main/resources" />
	<property name="connector-zip.name" value="BoomiCacheConnector-${version.num}.zip" />
	<property name="connector-xml.name" value="connector-descriptor.xml" />
	<property name="connector-sdk-home.dir" location="C:/Users/AnthonyRabiaza/OneDrive/MyDev/lib/boomi/connector-sdk-dist-1.3.2-dist"/>
	<property name="boomi-home.dir" location="C:/Users/AnthonyRabiaza/OneDrive/MyDev/lib/boomi"/>

	<property name="boomi-server" location="yourserver.domain.com" />
	<property name="boomi-user" location="user" />
	<property name="boomi-password" location="password" />
	<property name="local-deploy.dir" location="/opt/Boomi_AtomSphere/Atom/Atom_name/connector/boomiaccount-ABC123-cache-dev" />
	<property name="local-connector-zip.name" value="connector-boomiaccount-ABC123-cachev-dev-7900-car.zip" />
	<property name="local-connector-xml.name" value="info-boomi_boomiaccount-ABC123--cachev-dev-7900.xml" />

	<path id="full-classpath">
		<fileset dir="${connector-sdk-home.dir}">
			<include name="*.jar"/>
		</fileset>
		<fileset dir="${boomi-home.dir}">
			<include name="*.jar"/>
		</fileset>
		<fileset dir="lib">
			<include name="*.jar"/>
		</fileset>
	</path>

	<target name="clean">
		<delete dir="${target.dir}" />
	</target>

	<target name="info">
		<echo message="Project directory: ${basedir}" />
		<echo message="Build: ${build.number}" />
		<echo message="User: ${user.name}"/>
	</target>

	<target name="build">
		<javac srcdir="src/main/java"
			   destdir="bin"
			   fork="true"
			   source="1.8"
			   target="1.8"
			   debug="true"
			   includeantruntime="true"
		>
			<classpath refid="full-classpath"/>
		</javac>
	</target>

	<target name="inner-jar" depends="info,build">
		<manifest file="MANIFEST.MF">
			<attribute name="Built-By" value="${user.name}"/>
		    <attribute name="Implementation-Version" value="${version.num}-b${build.number}"/> 
		</manifest>
		<jar destfile="${target.dir}/${inner-jar.name}" manifest="MANIFEST.MF">
			<fileset dir="${class.dir}" excludes="**/test**" />
			<fileset dir="${embed-resources.dir}/." />
		</jar>
	</target>
	<target name="package" depends="inner-jar">
		<zip destfile="${target.dir}/${connector-zip.name}">
			<zipfileset dir="lib" prefix="lib"/>
			<zipfileset dir="${target.dir}" includes="${inner-jar.name}" prefix="lib"/>
			<zipfileset dir="${connector-sdk-home.dir}" includes="connector-sdk-util-?.?.?.jar" prefix="lib"/>
			<zipfileset dir="${basedir}/META-INF" includes="connector-config.xml" prefix="META-INF"/>				
		</zip>
		<copy file="${connector-xml.name}" todir="${target.dir}" />
		<delete file="${target.dir}/${inner-jar.name}" />
	</target>


	<target name="debug-deploy" depends="package">
		<!--
		<copy file="${target.dir}/${connector-xml.name}"  tofile="${local-deploy.dir}/${local-connector-xml.name}"/>
		<copy file="${target.dir}/${connector-zip.name}"  tofile="${local-deploy.dir}/${local-connector-zip.name}"/>
		-->
		<scp file="${target.dir}/${connector-xml.name}" todir="${boomi-user}@${boomi-server}:${local-deploy.dir}" password="${boomi-password}" trust="true"/>
	</target>
</project>